apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Node Feature Discovery — prerequisite для Intel GPU device plugin.
# Деплоится в namespace node-feature-discovery.
#
# ArgoCD: используйте project default (или расширьте AppProject ollama
# destination на namespace node-feature-discovery).
#
# Вручную:
#   kubectl apply -k nfd/

resources:
  - https://github.com/intel/intel-device-plugins-for-kubernetes/deployments/nfd?ref=v0.36.0
